'use client';

import styled from 'styled-components';

export const LogoWrapper = styled.div`
  img {
  }
`;

//object-fit: contain;
//border-radius: 12px;

//width: clamp(50px, 8vw, 120px);
//height: clamp(50px, 8vw, 120px); /* фиксируем высоту, можно настроить */
